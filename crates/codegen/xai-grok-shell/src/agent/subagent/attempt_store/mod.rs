//! No-caller canonical attempt records.

#[allow(
    dead_code,
    reason = "codec foundation is consumed by the next storage slice"
)]
mod codec;

#[cfg(test)]
#[path = "codec_tests.rs"]
mod codec_tests;
